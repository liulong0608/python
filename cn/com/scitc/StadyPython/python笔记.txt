def函数的定义和使用
	def  函数名（）：
		函数体
		return 
二、函数中的参数
1、位置参数
	max(num1.num2)
	value=max(10,20)
	10->num1
	20->num2
2、默认参数
	score(num1,num2=60)
	v1=score(80)
	v2=score(80,75)
3、关键字参数
	ma(num2=45,num1=60)
4、可变参数
    ls=[2,'hello',38]
    列表，元组，字典
    列表：def 函数名（*参数名，参数）
    字典：def 函数名（*参数名，参数）
    主要用于处理参数个数不确定的情况
    *序列拆分操作符

一、新建
    [].list()
    s=[]
    t=list()
    二、修改列表
    ls=[1,2,3,4,5[5,6]]
（1）修改列表中的元素
	ls[3]='你好'
	ls[1:4]=7,8,9
	ls[5][0]='abc'
（2）增加列表中的元素
    append(x):在列表最后一个元素后增加元素x
    insert(i,x)：在位置序号为i的位置增加元素x
    s.extend(t)：t是一个列表类型，把t列表中的元素添加在s中	=s+t
    三、删除列表中的元素
    remove(x)：把列表中x第一次出现的位置，x删除
    pop(i)：把列表中位置序号是i的元素删除
    del保留字：del[i],del[m:n],del[m,n,k]
    clear()：清空列表中的元素
四、copy()和赋值
五、列表的排序
    reverse()：把列表中的元素逆序
    sort()：默认把列表元素升序

六、字典
    创建字典使用大括号{}或dict（...），每个元素是一个键值对键和值通过冒号链接，不同键值用逗号分隔（字典键值对之间无序且不重复）


七、文件操作
    格式：     文件对象名=open(文件名，【打开方式，缓冲区】)
                open(file, mode='r', buffering=None, encoding=None, errors=None, newline=None, closefd=True)
    1、file：操作的文件
    2、mode：操作模式，默认为'r'，表示读模式。其它模式为
        'r':读模式 'w':写模式 'a':追加模式 'b':二进制模式（可添加到其它模式中使用，如rb表示二进制读，一般用来读一些视频或者音频文件）
         '+':读／写模式（可添加到其它模式中使用，如r+ 表示可读写）
    3、buffering：是否有缓冲，默认是无缓冲，如果传0或者False也是无缓冲，如果传1或者True默认是有缓冲。使用缓冲的好处是提高程序的执行速度，但是使用缓冲的话需要注意：一定要执行flush或者close时缓冲中的数据才会同步到硬盘上。如果传递大于1，如1024则代码缓冲的大小，表示1M。

    4、encoding：指明对文件编码，仅适用于文本文件。如果不明编码方式，默认是使用locale.getpreferredencoding()函数返回的编码方式。

    5、errors：指明编码和解码错误时怎么样处理。不能在二进制的模式下使用。
    6、newline：在文本模式之下时，控制一行的结束字符。可以是None，’’，\n，\r，\r\n等。

    7、closefd:是用来当给一个文件句柄传进来时，而当退出文件使用时，而不对文件句柄进行关闭。如果传递一个文件名进来，这个参数无效，必定为True方式。
